// user can send the number with rent cammand for session id and thepairing code is same for everyone that is "ASTROXV2" when the user sends !rent 917777777777 with their number the bot will create anew terminal and run $node pair on therminal and ask user to connect that pairing code with their whatsapp web and after that the bot will save the session file with the name of the number and run the bot with that session file and send a message to user that the bot is ready to use with that session and also send a message to owner that a new session is created with the number of the user who created it and also send a message to user if there is any error in creating the session or if the pairing code is wrong or if the user is not able to connect the pairing code with their whatsapp web and also send a message to user if the session is created successfully and also send a message to owner if there is any error in creating the session or if the pairing code is wrong or if the user is not able to connect the pairing code with their whatsapp web
/*const {default: makeWASocket, useMultiFileAuthState, DisconnectReason, jidNormalizedUser, getContentType, fetchLatestBaileysVersion, Browsers} = require("@eypzx/baileys")
const fs = require("fs");
const path = require("path");
const P = require("pino");
const config = require('../config');
const {Astro, commands} = require('../command');

Astro({
    pattern: "rent",
    desc: "rent for users"'
    filename: __filename
},
)*/